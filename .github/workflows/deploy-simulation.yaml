# name: Deploy WarpX Simulation

# on:
#   workflow_dispatch:
#     inputs:
#       simulation_type:
#         description: "Simulation Type"
#         required: true
#         default: "2d"
#         type: choice
#         options:
#           - "2d"
#           - "3d"
#       job_name:
#         description: "Unique Job Name (e.g., run-001)"
#         required: true
#         default: "warpx-run-1"

# permissions:
#   id-token: write # Required for OIDC
#   contents: read # Required to checkout code

# env:
#   AWS_REGION: us-east-2 # <--- UPDATE THIS
#   EKS_CLUSTER_NAME: duploinfra-dev # <--- UPDATE THIS
#   IAM_ROLE_ARN: arn:aws:iam::359835242613:role/GitHubAction-EKS-Deployer # <--- UPDATE THIS

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Code
#         uses: actions/checkout@v3

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: ${{ env.IAM_ROLE_ARN }}
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Update Kubeconfig
#         run: |
#           aws eks update-kubeconfig --name ${{ env.EKS_CLUSTER_NAME }} --region ${{ env.AWS_REGION }}

#       - name: Prepare Manifest
#         run: |
#           echo "Preparing ${{ inputs.simulation_type }} simulation..."

#           # Select the correct template
#           TEMPLATE_FILE="k8s-manifests/warpx-${{ inputs.simulation_type }}.yaml"
#           FINAL_MANIFEST="final-deployment.yaml"

#           # Define Output Directory Name
#           OUTPUT_DIR="output_${{ inputs.simulation_type }}_${{ inputs.job_name }}"

#           echo "Job Name: ${{ inputs.job_name }}"
#           echo "Output Dir: $OUTPUT_DIR"

#           # Replace Placeholders using sed
#           # We check if the job name already exists to avoid conflicts
#           if kubectl get mpijob ${{ inputs.job_name }} -n duploservices-dev > /dev/null 2>&1; then
#             echo "::error::Job '${{ inputs.job_name }}' already exists! Please choose a unique name."
#             exit 1
#           fi

#           cp $TEMPLATE_FILE $FINAL_MANIFEST

#           # Replace {{JOB_NAME}}
#           sed -i "s/{{JOB_NAME}}/${{ inputs.job_name }}/g" $FINAL_MANIFEST

#           # Replace {{OUTPUT_DIR}}
#           sed -i "s/{{OUTPUT_DIR}}/$OUTPUT_DIR/g" $FINAL_MANIFEST

#           echo "Manifest generated successfully."
#           cat $FINAL_MANIFEST

#       - name: Debug Identity
#         run: aws sts get-caller-identity

#       - name: Deploy to EKS
#         run: |
#           kubectl apply -f final-deployment.yaml --validate=false
#           echo "Deployment submitted!"

#       - name: Monitor Status (Optional)
#         run: |
#           echo "Waiting for Launcher pod to start..."
#           sleep 2
#           kubectl get pods -n duploservices-dev -l training.kubeflow.org/job-name=${{ inputs.job_name }}

name: Deploy WarpX Simulation

on:
  workflow_dispatch:
    inputs:
      simulation_type:
        description: "Simulation Type"
        required: true
        default: "2d"
        type: choice
        options:
          - "2d"
          - "3d"
      job_name:
        description: "Unique Job Name (e.g., run-001)"
        required: true
        default: "warpx-run-1"
      timeout:
        description: "Timeout in seconds (Default: 3600)"
        required: false
        default: "3600"

permissions:
  id-token: write # Required for OIDC
  contents: read # Required to checkout code

env:
  AWS_REGION: us-east-2
  EKS_CLUSTER_NAME: duploinfra-dev
  IAM_ROLE_ARN: arn:aws:iam::359835242613:role/GitHubAction-EKS-Deployer
  TARGET_NODE_LABEL: "node.kubernetes.io/instance-type=g5.12xlarge"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.IAM_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Update Kubeconfig
        run: |
          aws eks update-kubeconfig --name ${{ env.EKS_CLUSTER_NAME }} --region ${{ env.AWS_REGION }}

      # ---------------------------------------------------------
      # STEP 1: Node Availability Check
      # ---------------------------------------------------------
      - name: Check Node Availability
        run: |
          echo "Checking for READY nodes with label: ${{ env.TARGET_NODE_LABEL }}..."

          # Use || true to prevent grep failure (exit 1) if no nodes found
          NODE_COUNT=$(kubectl get nodes -l ${{ env.TARGET_NODE_LABEL }} --no-headers 2>/dev/null | grep -w "Ready" | wc -l)

          echo "Found $NODE_COUNT ready nodes."

          if [ "$NODE_COUNT" -eq "0" ]; then
            echo "::error::No READY nodes found with label ${{ env.TARGET_NODE_LABEL }}. Cannot schedule simulation."
            exit 1
          fi
          echo "Node check passed. Proceeding with deployment."

      # ---------------------------------------------------------
      # STEP 2: Prepare & Cleanup
      # ---------------------------------------------------------
      - name: Prepare Manifest
        run: |
          echo "Preparing ${{ inputs.simulation_type }} simulation..."

          TEMPLATE_FILE="k8s-manifests/warpx-${{ inputs.simulation_type }}.yaml"
          FINAL_MANIFEST="final-deployment.yaml"
          OUTPUT_DIR="output_${{ inputs.simulation_type }}_${{ inputs.job_name }}"

          echo "Job Name: ${{ inputs.job_name }}"
          echo "Output Dir: $OUTPUT_DIR"

          # Auto-Delete Old Job
          if kubectl get mpijob ${{ inputs.job_name }} -n duploservices-dev > /dev/null 2>&1; then
            echo "⚠️  Found existing job '${{ inputs.job_name }}'. Deleting it to start fresh..."
            kubectl delete mpijob ${{ inputs.job_name }} -n duploservices-dev --timeout=30s
            echo "✅ Old job deleted."
          else
            echo "No existing job found. Proceeding."
          fi

          # Prepare File
          cp $TEMPLATE_FILE $FINAL_MANIFEST

          # --- FIX IS HERE: Reverted to {{ }} to match your YAML files ---
          # Replace {{JOB_NAME}}
          sed -i "s/{{JOB_NAME}}/${{ inputs.job_name }}/g" $FINAL_MANIFEST

          # Replace {{OUTPUT_DIR}} (using | delimiter for paths)
          sed -i "s|{{OUTPUT_DIR}}|$OUTPUT_DIR|g" $FINAL_MANIFEST

          echo "Manifest generated successfully."

      - name: Debug Identity
        run: aws sts get-caller-identity

      - name: Deploy to EKS
        run: |
          kubectl apply -f final-deployment.yaml --validate=false
          echo "Deployment submitted!"

      - name: Monitor Status (Quick Check)
        run: |
          echo "Waiting for Launcher pod to start..."
          sleep 5
          kubectl get pods -n duploservices-dev -l training.kubeflow.org/job-name=${{ inputs.job_name }}

      # ---------------------------------------------------------
      # STEP 3: Wait for Completion
      # ---------------------------------------------------------
      - name: Wait for Simulation Completion
        run: |
          JOB_NAME="${{ inputs.job_name }}"
          NAMESPACE="duploservices-dev"
          TIMEOUT="${{ inputs.timeout }}"

          echo "Waiting for Launcher pod to initiate..."

          MAX_RETRIES=30
          COUNTER=0
          LAUNCHER_POD=""

          until [ ! -z "$LAUNCHER_POD" ] || [ $COUNTER -eq $MAX_RETRIES ]; do
             # Find pod with 'launcher' in name to handle label mismatches
             ALL_PODS=$(kubectl get pods -n $NAMESPACE -l training.kubeflow.org/job-name=$JOB_NAME -o jsonpath='{.items[*].metadata.name}' 2>/dev/null || true)
             for pod in $ALL_PODS; do
               if [[ "$pod" == *"launcher"* ]]; then
                 LAUNCHER_POD=$pod
                 break
               fi
             done

             if [ -z "$LAUNCHER_POD" ]; then
                echo "Launcher pod not found yet... retrying ($COUNTER/$MAX_RETRIES)"
                sleep 2
                COUNTER=$((COUNTER+1))
             fi
          done

          if [ -z "$LAUNCHER_POD" ]; then
             echo "::error::Launcher pod was not created within 60 seconds. Check MPIOperator logs."
             exit 1
          fi

          echo "Tracking Pod: $LAUNCHER_POD"
          START_TIME=$(date +%s)

          # Polling loop
          while true; do
            # Fetch Pod Phase
            PHASE=$(kubectl get pod $LAUNCHER_POD -n $NAMESPACE -o jsonpath='{.status.
